# Ubuntu / Debian packages (apt). One per line, # comments allowed.
# Installed by scripts/bootstrap.sh. Add what you want on Linux here.
zsh
git
stow
make
curl
unzip
tmux
fzf
bat
eza
zoxide
direnv
jq
gh
btop
zsh-autosuggestions
zsh-syntax-highlighting

# Installed by scripts/shell.sh and scripts/nvim.sh (distro versions too old):
#   neovim, starship, lazygit, tree-sitter-cli, go
# Not in apt - install manually if wanted:
#   git-delta  -> https://github.com/dandavison/delta/releases (.deb)
#   glow       -> https://github.com/charmbracelet/glow/releases
